export const JSStarterCodeSearchA2DMatrix = `/**
* @param {number[][]} matrix
* @param {number} target
* @return {boolean}
*/
var searchMatrix = function(matrix, target) {
   
};`

export const PyStarterCodeSearchA2DMatrix = `class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
    
  `
export const TSStarterCodeSearchA2DMatrix = `function searchMatrix(matrix: number[][], target: number): boolean {
    
};
  `

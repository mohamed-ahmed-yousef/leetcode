export const JSStarterCodeMergeIntervals = `/**
* @param {number[][]} intervals
* @return {number[][]}
*/
var merge = function(intervals) {
   
};`

export const PyStarterCodeMergeIntervals = `class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:

  `
export const TSStarterCodeMergeIntervals = `function merge(intervals: number[][]): number[][] {
    
};
  `

export const JSStarterCodeValidParentheses = `/**
* @param {string} s
* @return {boolean}
*/
var isValid = function(s) {
   
};`

export const PyStarterCodeValidParentheses = `class Solution:
        def isValid(self, s: str) -> bool:
    `
export const TSStarterCodeValidParentheses = `function isValid(s: string): boolean {
    
};
  `
